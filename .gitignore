# Ficheiros gerados / temporários
*.pyc
__pycache__/
*.tmp

# Documentos reais do cliente (templates/origens) — manter fora do repositório.
# Coloca aqui os teus .xlsm/.docx; usa --template / --input para os indicar.
examples/*.xlsm
examples/*.xlsx
examples/*.xlsm.tmp
examples/*.docx
examples/*.pdf

# Saídas geradas
out/
*_gerado.xlsm
